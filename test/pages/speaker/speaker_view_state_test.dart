import 'package:gonfi/domain/models.dart';
import 'package:gonfi/pages/speaker/speaker_view_state.dart';
import 'package:test/test.dart';

void main() {
  var speaker;

  setUp(() {
    speakerEntity = Speaker(

    );
    speaker = SpeakerViewState.fromEntity()
  });
  test('', () {});
}
